# Arena schema history

Local development does not require running these migrations. Arena boots its complete
schema automatically in embedded PGlite mode.

For hosted Postgres, use Drizzle migrations/push against the hosted DATABASE_URL.
